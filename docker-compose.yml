services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.dev
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
    stdin_open: true
    tty: true

networks:
  default:
    name: django-kokori-network
